import styled from 'styled-components';

const TodoItem = () => {
	return <Container>TodoItem</Container>;
};

export default TodoItem;

const Container = styled.div`
	width: 100%;
	padding: 15px;
	margin: 15px;
	border: 2px solid;
	border-radius: 12px;
`;
